# Changelog

## [1.1.0] - 2026-05-12

### 추가
- 익스텐션 팝업에서 저장된 메모 전체 목록 표시
- 팝업 내 유저명 및 메모 내용 실시간 검색 기능
- 팝업에서 메모 직접 삭제 기능
- 메모 저장 시 작성 시간(`createdAt`) 및 수정 시간(`updatedAt`) 기록
- 개드립 페이지 메모 영역에 수정 시간 표시
- 메모 입력/수정/삭제를 커스텀 인페이지 모달로 처리 (ESC 키 및 오버레이 클릭으로 닫기)
- `content.css` 파일 분리 — 인라인 스타일 제거

### 변경
- 기존 `prompt()` / `alert()` / `confirm()` → 커스텀 모달로 교체
- 팝업 페이지를 사용 설명서에서 메모 목록 뷰로 전환
- 스토리지 데이터 구조 변경: 문자열 → `{ text, name, createdAt, updatedAt }` 객체
  - 기존 문자열 메모는 자동으로 새 구조로 마이그레이션됨
- MutationObserver 콜백 내 불필요한 disconnect/reconnect 제거
- `chrome.runtime.lastError` 에러 핸들링 추가

## [1.0.0] - 2024-11-27

### 추가
- 최초 릴리즈
- 개드립 유저 팝업 메뉴에 메모 추가/수정/삭제 기능
- `chrome.storage.sync` 기반 메모 저장 및 기기 간 동기화
